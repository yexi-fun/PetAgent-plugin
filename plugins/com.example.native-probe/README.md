# Native probe

This sample implements PetAgent native ABI v1 and is loaded in-process after
architecture, export, initialization and health checks. Authenticode signatures
are not required in trusted-code mode.
