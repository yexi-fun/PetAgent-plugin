# Market metadata

`index.json` and `security/revocations.json` are generated release artifacts for
the reviewed first-party repository. This market mode does not require signing
keys; review, protected branches, same-repository Release URLs and SHA-256
checks are mandatory.
